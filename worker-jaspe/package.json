{
  "name": "schoolsafe-jaspe-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --import tsx --test test/worker.test.ts"
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  }
}
